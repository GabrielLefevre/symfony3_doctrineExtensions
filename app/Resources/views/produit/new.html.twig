{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    <input type="submit" value="Create" />
    {{ form_end(form) }}

    <a href="{{ path('produit_index') }}">Back to the list</a>
{% endblock %}